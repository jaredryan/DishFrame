-- This migration was auto-generated by `prisma migrate dev` immediately
-- after the prior 20260731173746_cooking_session_part_usage migration was
-- hand-edited to strip spurious DROP statements for hand-authored raw-SQL
-- objects (dish_current_version_ownership, ingredient/instruction/
-- part_link_section consistency CHECKs, the trigram indexes) that Prisma's
-- shadow-database diff doesn't know about — same documented issue as
-- SLICE_2.md §5.2 and this migration's own predecessor. `migrate dev`
-- generated a *second* migration re-proposing the exact same DROPs to
-- reconcile the shadow DB against the edited file, and applied it to the
-- local dev database before this was caught.
--
-- This file is intentionally emptied to a no-op so it never drops those
-- protected objects on any other database (CI, a fresh clone, production)
-- that applies migration history in order. The local dev database that had
-- already executed the drops was reset and cleanly reapplied from this
-- corrected history — see docs/SLICE_9.md's correction-pass section.
